ORG 8000H

SEG1 EQU 0FFC3h
SEG2 EQU 0FFC2h
SEG3 EQU 0FFC1h

START:
	MOV A, #01H
	MOV DPTR, #SEG1
	MOVX @DPTR, A
	
	MOV A, #23H
	MOV DPTR, #SEG2
	MOVX @DPTR, A

	MOV A, #45H
	MOV DPTR, #SEG3
	MOVX @DPTR, A

	JMP $